name = "abuse-email-worker"
main = "src/index.js"
compatibility_date = "2024-12-01"

[vars]
GITHUB_REPO = "ChainSafe/orbitor-ipfs-denylist"
FALLBACK_EMAIL = "abuse@chainsafe.io"

# Secrets (set via `wrangler secret put`):
# GITHUB_TOKEN - GitHub PAT with repo issues write access
