{
  "name": "abuse-email-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Email Worker — parses abuse reports for IPFS CIDs and creates GitHub Issues",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "multiformats": "^13.3.1",
    "postal-mime": "^2.3.2"
  },
  "devDependencies": {
    "wrangler": "^3.99.0"
  }
}
